# Base URL of the REST API the dashboard talks to.
# Locally this points at the json-server mock API (see README).
NEXT_PUBLIC_API_URL=http://localhost:4000

# Secret used to sign the login session cookie. Generate your own random string,

AUTH_SECRET=admindasboard3625876
